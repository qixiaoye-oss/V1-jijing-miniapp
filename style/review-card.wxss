/**
 * 答题结果卡片公共样式
 * @description 用于 paper_based_review / computer_based_review / result_list 页面
 *
 * 颜色状态：
 * - 默认/正确：蓝色系 (--main-blue-color)
 * - 错误/自动标记：红色系 (--main-red-color)
 * - 正确+手动标记：橘色系 (--main-orange-color)
 */

/* 页面内容区域 */
/* 注意：padding-bottom 需在页面级 wxss 中设置 page 元素，而非此处 */

/* ==================== 题目卡片 ==================== */

/* 默认蓝色系 */
.question-card {
  border: 1px solid var(--main-blue-color);
  border-radius: 9px;
  margin-bottom: 15px;
  overflow: hidden;
}

/* 最后一个卡片不需要底部间距（由蒙版B提供15px渐变间距） */
.question-card:last-child {
  margin-bottom: 0;
}

/* 错误状态 - 红色系 */
.question-card--wrong {
  border-color: var(--main-red-color);
}

/* 已标记状态 - 橘色系 */
.question-card--marked {
  border-color: var(--main-orange-color);
}

/* ==================== 标题行 ==================== */

.question-header {
  display: flex;
  align-items: center;
  padding: 15px;
  gap: 10px;
  border-bottom: 1px solid var(--main-blue-color);
}

.question-card--wrong .question-header {
  border-bottom-color: var(--main-red-color);
}

.question-card--marked .question-header {
  border-bottom-color: var(--main-orange-color);
}

/* 序号 - 半透明背景+文字 */
.question__no {
  background-color: var(--main-blue-color-bg);
  color: var(--main-blue-color);
  flex-shrink: 0;
  width: 28px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
  border-radius: 3px;
  font-size: 12px;
}

.question-card--wrong .question__no {
  background-color: var(--main-red-color-bg);
  color: var(--main-red-color);
}

.question-card--marked .question__no {
  background-color: var(--main-orange-color-bg);
  color: var(--main-orange-color);
}

/* 标题 */
.question__title {
  flex: 1;
  font-size: 15px;
  color: var(--main-blue-color);
}

.question-card--wrong .question__title {
  color: var(--main-red-color);
}

.question-card--marked .question__title {
  color: var(--main-orange-color);
}

/* 状态文字 */
.question__status {
  font-size: 12px;
  color: var(--main-blue-color);
}

.question-card--wrong .question__status {
  color: var(--main-red-color);
}

.question-card--marked .question__status {
  color: var(--main-orange-color);
}

/* 灰色状态文字（未标记） */
.question__status--gray {
  color: var(--main-gray-color);
}

/* ==================== 单词区域 ==================== */

.word-section {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  padding: 15px;
  border-bottom: 1px solid var(--main-blue-color);
}

.question-card--wrong .word-section {
  border-bottom-color: var(--main-red-color);
}

.question-card--marked .word-section {
  border-bottom-color: var(--main-orange-color);
}

.word-content {
  flex: 1;
}

.word_text {
  font-size: 22px;
  font-weight: bold;
  margin-right: 10px;
  color: var(--main-blue-color);
}

.word_translate {
  font-size: 14px;
  color: #999999;
  margin-top: 5px;
}

/* ==================== 例句区域 ==================== */

.sentence-section {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  padding: 15px;
}

.sentence-content {
  flex: 1;
  padding-right: 10px;
}

.sentence_text {
  font-size: 17px;
  line-height: 24px;
  font-weight: bold;
}

.sentence_translate {
  font-size: 14px;
  color: #999999;
  line-height: 20px;
  margin-top: 8px;
}

/* ==================== 播放按钮 ==================== */

.play-btn {
  flex-shrink: 0;
  display: flex;
  align-items: center;
}
